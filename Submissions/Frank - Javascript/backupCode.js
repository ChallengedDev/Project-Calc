// setInterval(() => {
//     console.log(info.textContent, clock, clock[0]);
// }, 2000);

// if (e.key === "Backspace") {
//       console.log("exception");
//     } else {
//       if (isaNumber(letter, ref)) {
//           typed = typed.concat(letter);
//           clock.pop()
//           clock.push(typed.concat(padInput))
//       } else {
//         typed = typed.concat("");
//         info.textContent = "No letters allowed";
//       }
//     }

//     input.value = clock[0];


  // typed = isaNumber(letter, ref)
  //   ? typed.concat(letter)
  //       : typed.concat("");

  //     //keep value to digits by quickly resetting the input's value
  // console.log("clockin: ",clock)
  //     if (clock === 2) {
  //       resetter = setInterval(() => {
  //         console.log(typed);
  //         //   input.value = typed.concat(padInput) || padInput.concat(typed);
  //         // console.log(input.value)
  //       },5000);
  //     }